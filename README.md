## Rails Application Templates

プロジェクトの作成後、いつも同じgemを追加する手間を自動化するアプリケーションテンプレートです。
`rails new -m <TEMPLATE>` の形式で `-m` オプションを付けて、コマンドの引数にテンプレートファイルを指定して実行します。

```ruby
$ rails new アプリ名 -m template.rb
```
